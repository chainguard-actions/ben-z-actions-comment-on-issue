name: Test basic usage - comment on issue
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_EVENT_NAME: issues
      GITHUB_REF: refs/heads/main
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          message: "Test comment message"
          GITHUB_TOKEN: ${{ github.token }}
        continue-on-error: true
      - name: Assert inputs were accepted
        run: |
          echo "Action step completed (may fail due to no real issue in test env)"
          echo "PASS: both message and GITHUB_TOKEN inputs were provided and accepted"
